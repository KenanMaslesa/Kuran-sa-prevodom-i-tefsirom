import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coding.kuran',
  appName: 'Kur\'an',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
